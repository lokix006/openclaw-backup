# 2026-03-11 Daily Notes

## repo-monitor skill 完成

- skill 路径：`/root/.openclaw/workspace/ikoL/skills/repo-monitor/`
- SKILL.md 经历两次迭代：
  1. 初版用 cron 委托 main 执行写操作 → 失败（ikol 无法跨 agent 创建 cron job）
  2. 第二版改用 sessions_spawn → 安全风险（spawn 出的 subagent 继承 main 完整权限）
  3. 最终版：ikol 直接用 write/edit 操作 CSV（文件在 workspaceOnly 范围内），手动触发告知等待 5 分钟 cron 即可
- SKILL.md 里明确禁止读取 .env 内容
- 已加入 skills.load.extraDirs，skill 状态 ready

## agent 配置变更（2026-03-11）

- ikuncode provider 已全局移除（provider/fallbacks/models 白名单均清理）
  - 原因：ikuncode 内容审核 WAF 激进，session context 含敏感信息时持续返回 403
- ikol model 改为 `kellycloudai/claude-sonnet-4-6`
- 新增 kellycloudai provider：`https://www.kellycloudai.com/v1`
  - 模型：claude-sonnet-4-6、gpt-5.4，api: openai-completions

## SOUL.md 安全加固

- ikol 和 claw-wiki 的 SOUL.md 均加入规则：
  - 禁止自行修改配置文件（SOUL/IDENTITY/AGENTS/MEMORY 等）
  - 所有配置变更必须通过 main agent 操作

## OpenRouter 余额告警（已处理）

- 2026-03-11 凌晨所有 cron job 因 402 insufficient credits 连续失败
  - Repo Monitor：consecutiveErrors 14
  - Agent Security Audit：consecutiveErrors 12
- Loki 已充值，jobs 恢复正常

## repos.csv 更新

- Loki 通过 ikol 新增仓库：`lokix006/relayer`，main 分支，仅通知 Loki
- 两个收件人：`ou_570aeb8842a1cbbc0313861d2b5c128f;ou_4da47605a07e3c09bb41b6177b2d35ea`

## 语言漂移问题

- 本次会话出现两次语言漂移（日文），原因：context 过长（355k tokens）
- 处理方式：/compact 压缩到 24k，后续注意在 context 过大时主动建议 compact

## Exa Search / Daily Brief 调整

- Exa Search 旧方案确认问题点：搜索本身可运行，但 Feishu 发送阶段会因 `message.send` 混入 `poll*` 字段失败。
- 发送链路已改成稳定方案：优先使用 `openclaw message send` CLI，避免 message tool 参数污染。
- `ikoL/skills/exa-search/scripts/search.py` 曾被改成 `search + summary/highlights` 聚合，但与 Loki 原始需求不一致；后续已改回以 `Exa.answer(...)` 为主的中文输出路线。
- Loki 明确决定：前面的 exa-search skill 设计后续准备推翻重构；以从官网拿到的 TS 版本为基线，明天重点重构数据源与内容生成层。

## Cron / Job 修复（2026-03-11 下午）

- `Repo Monitor` job 已重构：
  - 保留 `monitor.py` 只负责检查仓库和写待发送队列
  - 新增 `ikoL/projects/repo-monitor/send_pending.py`，直接用 OpenClaw CLI 发送 Feishu 并清理已发送项
  - cron payload 已改为“运行 monitor.py + 运行 send_pending.py”，不再让模型逐条读 JSON 再发消息
  - 手动执行后，`pending_notifications.json` 已清空
- `Agent Security Audit` job 已重构：
  - 新增 `scripts/agent_security_audit.py`
  - 改为按 `audit-state.json` 增量扫描 `ikol`/`claw-wiki` session 文件
  - 有告警时再通过 OpenClaw CLI 发 Telegram；无告警输出 `NO_ALERTS`
  - 手动执行脚本结果正常
- 为解决 delivery 状态与真实投递不一致，以下 job 的 `delivery.mode` 已统一改为 `announce`：
  - `Daily System Inspection`
  - `Exa OpenClaw Daily Brief`
  - `OpenClaw Daily Brief (GTM Agent)`
  - `OpenClaw Daily Brief (Group)`
- 上述 daily/brief 类 job 的 prompt 也已调整为：**不要调用消息发送工具，只输出最终正文，由 cron delivery 负责投递**。

## 当前配置认知

- 当前全局默认模型为 `kellycloudai/gpt-5.4`（来自 `agents.defaults.model.primary`）
- `ikol` 与 `claw-wiki` 当前显式使用 `openrouter/anthropic/claude-sonnet-4.6`
- Loki 明确表示：模型不统一暂时不用处理；后续优先解决 job 错误和 delivery 一致性
- 密钥现状：主配置里仍有多类明文密钥；后续建议迁移到独立 `.env.secrets` / systemd `EnvironmentFile` 方案
